<table class="table">
  <thead>
    <tr>
      <th scope="col">
        Tarea
      </th>
      <th scope="col">
        Título
      </th>
      <th scope="col">
        Descripción
      </th>
      <th scope="col">
        Operaciones
      </th>
    </tr>
    <tbody>
      {{#each tasks}}
        <tr scope="row">
          <td>{{@index}}</td>
          <td>{{title}}</td>
          <td>{{description}}</td>
          <td>
            {{#if done}}
                <a href="/toggleDone/{{_id}}">Hecha</a>
            {{else}}
                <a href="/toggleDone/{{_id}}">Sin hacer</a>
            {{/if}}
            <a href="/delete/{{_id}}">Eliminar</a>
            <a href="/edit/{{_id}}">Actualizar</a>
          </td>
        </tr>
      {{/each}}
    </tbody>
  </thead>
</table>
